# رفع المشروع إلى GitHub واستبدال المستودع الحالي

المستودع الهدف: **https://github.com/bajaher/income_tax_exam**

جهّزتُ لك المجلد كمستودع git جاهز (تهيئة + أول commit + ربط الـ remote).
لم يبقَ سوى أمر واحد ترفعه أنت — لأن الرفع يحتاج تسجيل دخولك لحسابك، ولا يجوز لي التعامل مع رموز الدخول.

> الأمر أدناه يقوم أيضًا بحفظ (commit) آخر التحديثات الموجودة في المجلد قبل الرفع، فلا حاجة لأي خطوة إضافية.

---

## الخطوة الوحيدة المطلوبة

افتح تطبيق **Terminal** على الماك، والصق الأمر التالي كما هو (سطر واحد ينفّذ كل شيء):

```bash
cd ~/Desktop/T\&Z/INCOME_TAX/exam-simulator && rm -f .git/*.lock && git add -A && git commit -m "تحديث محاكي اختبار الجاهزية" ; git push -u origin main --force
```

> الأمر يبدأ بحذف ملفات القفل المؤقتة (`.git/*.lock`) لأن مجلد سطح المكتب لا يسمح لي بحذفها من طرفي — وهي فارغة وغير مؤثرة.
> إن ظهرت رسالة `nothing to commit` فهذا طبيعي ويعني أن كل شيء محفوظ، وسيتابع الأمر الرفع.

- سيطلب منك GitHub اسم المستخدم وكلمة المرور:
  - **Username:** `bajaher`
  - **Password:** ألصق هنا **Personal Access Token** وليس كلمة مرور حسابك
    (أنشئه من: GitHub ← Settings ← Developer settings ← Personal access tokens ← Tokens (classic) ← Generate new token ← فعّل صلاحية **repo**)
- إن كان جهازك يستخدم GitHub Desktop أو مفتاح SSH فسيتم الرفع مباشرة دون سؤال.

> `--force` تستبدل محتوى المستودع القديم بالكامل بالمشروع الجديد، وهو المطلوب حسب طلبك.
> إن أردت الإبقاء على تاريخ المستودع القديم فاحذف `--force` واستخدم `git pull --rebase origin main` أولًا.

---

## تفعيل رابط الاختبار (GitHub Pages)

بعد نجاح الرفع:

1. افتح `https://github.com/bajaher/income_tax_exam/settings/pages`
2. **Source** = `Deploy from a branch` • **Branch** = `main` • **Folder** = `/ (root)` ← ثم **Save**
3. بعد دقيقة يصبح رابط الطلاب جاهزًا:

```
https://bajaher.github.io/income_tax_exam/
```

4. افتح ملف `qr.html` من المجلد، والصق الرابط أعلاه لتوليد رمز QR للطلاب.

---

## ⚠️ تنبيه أمني مهم قبل الرفع

مستودعات GitHub Pages المجانية **عامة**، أي أن أي شخص يستطيع فتح ملف `config.js` وقراءة:

- كلمة مرور الطلاب: `KKU2026`
- كلمة مرور المعلم: `ADMIN-KKU2026`

**التوصية:**

- استخدم هذا الرابط للتدريب والجاهزية فقط، لا لاختبار رسمي مرصود بدرجات.
- غيّر كلمتي المرور قبل كل اختبار من `config.js` أو من لوحة المعلم.
- للاختبارات الرسمية استخدم البلاكبورد أو نظام الجامعة.

بديل أكثر خصوصية: اجعل المستودع **Private** واستخدم استضافة تدعم الحماية بكلمة مرور
(مثل Netlify بخاصية Password Protection في الخطط المدفوعة).

---

## تحديث المشروع لاحقًا

بعد أي تعديل على الملفات:

```bash
cd ~/Desktop/T\&Z/INCOME_TAX/exam-simulator
git add -A
git commit -m "وصف التعديل"
git push
```
